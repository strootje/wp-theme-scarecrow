import { h, Component } from 'preact';
import types from 'prop-types';
import names from 'classnames';

class PageError404View extends Component {
	render({ styles }) {
		return (
			<div>error 404</div>
		);
	}
}

export default PageError404View;
